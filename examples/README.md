# Examples

Minimal examples demonstrating provider features.

## [basic/](basic/)
Simple JSON Schema validation with custom error templates.

## [ref_overrides/](ref_overrides/)
Validating schemas with remote `$ref` URLs using local files (offline validation).

## Running Examples

```bash
cd <example-directory>
terraform init
terraform plan
```

See each example's README for details.
